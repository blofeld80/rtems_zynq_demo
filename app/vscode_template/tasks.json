{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start QEMU (waiting for GDB connection)",
      "type": "shell",
      "isBackground": true,
      "command": "REPLACE_TOOL_INSTALL_DIR/qemu/bin/qemu-system-aarch64 -M arm-generic-fdt-7series -serial /dev/null -serial mon:stdio -display none -m 1024M -dtb REPLACE_BOARD_INSTALL_DIR/dtb/picozed-zynq7.dtb -s -S",
    }
  ]
}